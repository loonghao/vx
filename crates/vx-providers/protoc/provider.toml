# Protoc Provider Manifest
# This file defines the metadata, runtimes, and dependency constraints for Protocol Buffers.

[provider]
name = "protoc"
description = "Protocol Buffers compiler"
homepage = "https://protobuf.dev"
repository = "https://github.com/protocolbuffers/protobuf"
ecosystem = "devtools"

# protoc
[[runtimes]]
name = "protoc"
description = "Protocol Buffers compiler"
executable = "protoc"

[runtimes.versions]
source = "github-releases"
owner = "protocolbuffers"
repo = "protobuf"
strip_v_prefix = true

# RFC 0019: Executable Layout Configuration
# protoc downloads are archives with bin/ directory
[runtimes.layout]
download_type = "archive"

[runtimes.layout.archive]
strip_prefix = ""  # protoc archives don't have version prefix
executable_paths = [
    "bin/protoc.exe",  # Windows
    "bin/protoc"       # Unix
]

[runtimes.platforms.windows]
executable_extensions = [".exe"]

[runtimes.platforms.unix]
executable_extensions = []

# protoc has no external dependencies
[[runtimes.constraints]]
when = "*"
recommends = []
