# Terraform Provider Manifest
# This file defines the metadata, runtimes, and dependency constraints for Terraform.

[provider]
name = "terraform"
description = "Infrastructure as Code tool"
homepage = "https://www.terraform.io"
repository = "https://github.com/hashicorp/terraform"
ecosystem = "devtools"
license = "BSL-1.1"
license_note = "HashiCorp BSL; vx only downloads and manages versions, not competing product"

# Terraform
[[runtimes]]
name = "terraform"
description = "Terraform CLI"
executable = "terraform"

[runtimes.versions]
source = "github-releases"
owner = "hashicorp"
repo = "terraform"
strip_v_prefix = true

# RFC 0019: Executable Layout Configuration
[runtimes.layout]
download_type = "archive"

[runtimes.layout.archive]
strip_prefix = ""
executable_paths = [
    "terraform.exe",  # Windows (root directory)
    "terraform"       # Unix (root directory)
]

[runtimes.platforms.windows]
executable_extensions = [".exe"]

[runtimes.platforms.unix]
executable_extensions = []

# Terraform has no external dependencies
[[runtimes.constraints]]
when = "*"
recommends = []
