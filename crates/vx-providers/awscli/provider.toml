# AWS CLI Provider Manifest
# This file defines the metadata, runtimes, and dependency constraints for AWS CLI.

[provider]
name = "awscli"
description = "Unified command line interface to Amazon Web Services"
homepage = "https://aws.amazon.com/cli/"
repository = "https://github.com/aws/aws-cli"
ecosystem = "cloud"

# AWS CLI v2
[[runtimes]]
name = "aws"
description = "AWS Command Line Interface"
executable = "aws"
aliases = ["awscli", "aws-cli"]

[runtimes.versions]
source = "github-releases"
owner = "aws"
repo = "aws-cli"
strip_v_prefix = false

# AWS CLI v2 detection configuration
[runtimes.detection]
command = "{executable} --version"
pattern = "aws-cli/([\\d.]+)"

# Layout configuration for MSI installation
[runtimes.layout]
download_type = "msi"

[runtimes.layout.msi."windows-x86_64"]
url_template = "https://awscli.amazonaws.com/AWSCLIV2-{version}.msi"
# AWS CLI MSI extracts to Amazon/AWSCLIV2/ directory structure
executable_paths = [
    "Amazon/AWSCLIV2/aws.exe",
    "aws.exe",
    "**/aws.exe"
]

[runtimes.platforms.windows]
executable_extensions = [".exe", ".cmd"]

[runtimes.platforms.unix]
executable_extensions = []

# AWS CLI has no external dependencies
[[runtimes.constraints]]
when = "*"
recommends = []
