# Ollama Provider Manifest
# This file defines the metadata, runtimes, and dependency constraints for Ollama.

[provider]
name = "ollama"
description = "Get up and running with large language models locally"
homepage = "https://ollama.ai"
repository = "https://github.com/ollama/ollama"
ecosystem = "ai"
license = "MIT"

# Ollama
[[runtimes]]
name = "ollama"
description = "Ollama LLM runner"
executable = "ollama"

[runtimes.versions]
source = "github-releases"
owner = "ollama"
repo = "ollama"
strip_v_prefix = true

# RFC 0020: Download Configuration
# Ollama is a large file (~200MB+), needs longer timeout
[runtimes.download]
timeout_ms = 900000  # 15 minutes for large file
max_retries = 5
resume_enabled = true
execution_timeout_ms = 60000  # 1 minute (ollama can take time to initialize)

# Note: Ollama uses platform-specific installers (.exe, .app, .deb)
# Layout configuration will be added when installer support is implemented

[runtimes.platforms.windows]
executable_extensions = [".exe"]

[runtimes.platforms.unix]
executable_extensions = []

# Ollama has no external dependencies
[[runtimes.constraints]]
when = "*"
recommends = []
