[workspace]
# Enable automatic changelog generation
changelog_update = true
# Enable automatic version bumping
git_release_enable = true
# Enable automatic tag creation
git_tag_enable = true

# Changelog configuration
[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

"""

# Package-specific configuration - only for the main vx CLI
[[package]]
name = "vx"
# Main CLI package is in the root directory (no path needed)
changelog_update = true
# Standard tag format for main CLI (triggers tag-release-assets.yml workflow)
git_tag_name = "v{{version}}"

# Note: Internal libraries (vx-core, vx-cli, vx-shim, vx-tool-*, vx-pm-*) are not configured here
# as they are internal components that follow the main package version and don't need separate releases
