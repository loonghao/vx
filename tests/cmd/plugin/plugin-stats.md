# Test: vx plugin stats

Verify that `vx plugin stats` shows plugin statistics.

```console
$ vx plugin stats

[..]
  Total providers: 7
  Total runtimes: 13

  Providers:
    node (3 runtimes)
    go (1 runtimes)
    rust (3 runtimes)
    uv (2 runtimes)
    bun (2 runtimes)
    pnpm (1 runtimes)
    yarn (1 runtimes)

```
