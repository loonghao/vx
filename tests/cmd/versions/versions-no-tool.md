# Test: vx versions (no tool)

Verify that `vx versions` without tool name shows error.

```console
$ vx versions
? failed
error: the following required arguments were not provided:
  <TOOL>

Usage: vx[EXE] versions <TOOL>

For more information, try '--help'.

```
