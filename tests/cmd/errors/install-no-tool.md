# Test: vx install (no tool)

Verify that `vx install` without tool name shows error.

```console
$ vx install
? failed
error: the following required arguments were not provided:
  <TOOL>

Usage: vx[EXE] install <TOOL> [VERSION]

For more information, try '--help'.

```
