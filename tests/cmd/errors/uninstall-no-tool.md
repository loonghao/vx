# Test: vx uninstall (no tool)

Verify that `vx uninstall` without tool name shows error.

```console
$ vx uninstall
? failed
error: the following required arguments were not provided:
  <TOOL>

Usage: vx[EXE] uninstall <TOOL> [VERSION]

For more information, try '--help'.

```
