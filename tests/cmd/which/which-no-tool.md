# Test: vx which (no tool argument)

Verify that `vx which` without tool name shows error.

```console
$ vx which
? failed
error: the following required arguments were not provided:
  <TOOL>

Usage: vx[EXE] which <TOOL>

For more information, try '--help'.

```
